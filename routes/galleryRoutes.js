const e=require('express'),c=require('../controllers/galleryController'),{protectAdmin}=require('../middleware/adminAuthMiddleware'),{requirePermission}=require('../middleware/permissionMiddleware'),{PERMISSIONS:P}=require('../constants/permissions');const r=e.Router(),view=requirePermission(P.GALLERY.VIEW),upload=requirePermission(P.GALLERY.UPLOAD),edit=requirePermission(P.GALLERY.EDIT),del=requirePermission(P.GALLERY.DELETE);r.use(protectAdmin);r.get('/albums/stats',view,c.albumStats);r.get('/albums',view,c.adminAlbums);r.post('/albums',edit,c.createAlbum);r.patch('/albums/:albumId/reorder',edit,c.reorder);r.get('/albums/:id',view,c.albumDetail);r.patch('/albums/:id',edit,c.updateAlbum);r.post('/albums/:id/publish',edit,c.publishAlbum);r.post('/albums/:id/unpublish',edit,c.unpublishAlbum);r.post('/albums/:id/archive',del,c.archiveAlbum);r.post('/albums/:id/restore',del,c.restoreAlbum);r.get('/media/stats',view,c.mediaStats);r.get('/media',view,c.adminMedia);r.post('/media',upload,c.addMedia);r.get('/media/:id',view,c.mediaDetail);r.patch('/media/:id',edit,c.updateMedia);r.patch('/media/:id/status',edit,c.setMediaStatus);r.patch('/media/:id/flags',edit,c.flags);r.post('/media/:id/archive',del,c.archiveMedia);r.post('/media/:id/restore',del,c.restoreMedia);module.exports=r;
